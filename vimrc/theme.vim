set t_Co=256

" Change this to change the color scheme. This is not setting the
" colour scheme (as the VIM setting is `colorscheme`), but it sets
" a variable used in `./settings/theme_<THEME_NAME>.vim`.
" let colour_scheme = "everforest"
" let colour_scheme = "gruvbox"
let colour_scheme = "gruvbox-material"
" let colour_scheme = "slate"
